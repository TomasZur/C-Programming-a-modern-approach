#include <stdio.h>
#define FRACTION (4.0/3.0)
#define PI (3.14)
#define TAX (0.05)


int main()
{
    //1 uzd

    /*
    printf("Hello, world\n");
    return 0;
    */

   ////////////////////////////////

   //2 uzd


   /*
   printf("Parkinson's Law:\nWork expans so as to ");
   printf("fill the time\n");
   printf("available for its completion.\n");
   return 0;
   */
   
    //Directives yra #include <stdio.h>
    //Statements yra visi printf

    ////////////////////////////////////

    //3 uzd
    

    /*
    printf("        *\n       *\n      *\n*    *\n *  *\n  *");
    return 0;
    */

    //4 uzd
    /*
       int height, length, width, volume, weight;
    


    height = 8;
    length = 12;
    width = 10;

    volume = height*length*width; 
    weight = (volume+165)/166;

    printf("Dimensions: %dx%dx%d\n", length,width,height);
    printf("Volume: %d\n", volume);
    printf("Dimensional weight %d\n", weight);

    printf("Skaiciavimas: %d", (volume+volume)/volume);
    */

    //5 uzd

    /*
    float volume;
    int radius = 10;

    volume = FRACTION*PI*(radius*radius*radius);

    printf("The volume of the sphere is: %f",volume);
    return 0;
    */

    //6 uzd

    /*
    int a, b, c;
    float d, e, f;
    printf("Sveikieji skaiciai: %d, %d, %d\n",a,b,c);
    printf("%f, %f, %f",d,e,f);
    return 0;
    */
   
    //7 uzd

    /*
    float volume;
    int radius;

    
    printf("Enter the radius of the sphere \n");
    scanf("%d",&radius);

    volume = FRACTION*PI*(radius*radius*radius);

    printf("The volume of the sphere is: %f",volume);
    return 0;
    */
  
    //8 uzd
    /*
    float money;
    printf("Enter the amount of money: \n");
    scanf("%f",&money);

    float money_tax=money+(money*TAX);

    printf("Money with tax: %.2f",money_tax);
    return 0;
    */


    
}